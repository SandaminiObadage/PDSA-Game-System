# Frontend

React + Vite frontend for the PDSA Game System.

## Run

```bash
cd frontend
npm install
npm run dev
```

The app connects to the backend at `http://localhost:8080/api/games/sixteen-queens`.
